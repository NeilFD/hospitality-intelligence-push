
import MonthSummary from '../MonthSummary';

export default function BeverageMonthSummary() {
  // Explicitly set moduleType to 'beverage' to ensure consistency
  return <MonthSummary modulePrefix="Beverage" moduleType="beverage" />;
}
